"""
ESERCIZI SULLE FUNZIONI RICORSIVE IN PYTHON
------------------------------------------
Ogni esercizio richiede la scrittura di una funzione ricorsiva.
Ricorda di definire sempre il "caso base" per evitare loop infiniti.
"""

# 1. Calcola il Fattoriale
# Scrivi una funzione ricorsiva `fattoriale(n)` che restituisca il prodotto
# di tutti i numeri da 1 a n.
# Esempio: fattoriale(5) -> 120 (5 * 4 * 3 * 2 * 1)


# 2. Somma dei primi N numeri
# Scrivi una funzione ricorsiva `somma_n(n)` che calcoli la somma di tutti 
# i numeri interi da 1 fino a n.
# Esempio: somma_n(5) -> 15 (5 + 4 + 3 + 2 + 1)


# 3. Elevamento a Potenza
# Scrivi una funzione ricorsiva `potenza(base, esponente)` che calcoli
# il risultato di base elevato a esponente.
# Esempio: potenza(2, 3) -> 8


# 4. Sequenza di Fibonacci
# Scrivi una funzione ricorsiva `fibonacci(n)` che restituisca l'n-esimo
# numero della sequenza di Fibonacci.
# Esempio: fibonacci(6) -> 8 (Sequenza: 0, 1, 1, 2, 3, 5, 8...)


# 5. Invertire una Stringa
# Scrivi una funzione ricorsiva `inverti_stringa(s)` che prenda una stringa
# e la restituisca invertita.
# Esempio: inverti_stringa("python") -> "nohtyp"


# 6. Somma degli elementi di una lista
# Scrivi una funzione ricorsiva `somma_lista(lista)` che restituisca
# la somma di tutti i numeri contenuti in una lista.
# Esempio: somma_lista([1, 2, 3, 4]) -> 10


# 7. Conteggio delle cifre
# Scrivi una funzione ricorsiva `conta_cifre(n)` che restituisca il numero
# di cifre di cui è composto un numero intero positivo.
# Esempio: conta_cifre(12345) -> 5


# 8. Verifica Palindromo
# Scrivi una funzione ricorsiva `is_palindromo(parola)` che restituisca
# True se la parola è uguale se letta al contrario, False altrimenti.
# Esempio: is_palindromo("radar") -> True